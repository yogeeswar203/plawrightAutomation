import test from "@playwright/test";

test.describe("Verifying the mouse actions",()=>{

    test.beforeEach("Navigating to Home page before the test case executions",()=>{

    })

    test("TC_001: Verifing the mouse actions demo 1",async({page})=>{

    })

})